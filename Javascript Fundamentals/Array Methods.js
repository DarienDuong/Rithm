arr = []
arr.unshift("Darien")
arr.unshift("Duong")
arr.unshift("green")

arr.shift()
arr2 = []
arr2.unshift(7)
arr2.push("Javascript")
arr.indexOf(42) will return -1 if the value is not in the array
var combinedArr = arr1.concat(arr2)

arr.slice(1,3)
arr.concat("Haskell", "Clojure")
arr.slice(0,4)
When a value is passed by value a variable creates a reference in memory to the value
When passed by reference the variable refers to the original value
